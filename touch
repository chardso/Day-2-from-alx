#include "main.h"
/**
 * _isupper - returns 1 if the upper case else 0
 * Return: 0
 * @c: is to be passed into the function
 * checks if character is upper case
 * /
init _isupper(int c)
{
      if _isupper(c)
      return (1);
} 
else
{
      return (0);
}
}