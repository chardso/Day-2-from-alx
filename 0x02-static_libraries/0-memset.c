#include "main.h"
/**
 * memset -fills memory with aconstant byte
 * @s: pointer of the memory location where the memory will be set.
 * @b: constant byte
 * @n: byte of the memory area pointed by s.
 * Return:0
 * /
   char *_ memset(char *s,char b,unsigned int n)
{
          unsigned int i = 0;
          /*while i is less than n bytes*/
          while (i <)
           {
                 /*b will be copied to the corresponding memory block*/
                 s[i] = b;
                 i++;
           }
           return (s);     
                    
           
           

           
